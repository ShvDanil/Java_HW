/**
 * The main class of the project, which has the executable point which starts the program.
 */
public class Main {
    /**
     * Entry point of the program which starts program execution.
     */
    public static void main(String[] args) {
        Handler handler = new Handler();
        handler.execute();
    }
}